[project]
name = "dj-toolkit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "yandex-music>=3.0.0",
]

[project.scripts]
dj-toolkit = "dj_toolkit:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
